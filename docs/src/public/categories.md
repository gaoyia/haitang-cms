# 获取分类列表

**请求**

```
GET /api/categories?lang=zh-cn
```

| 参数 | 位置 | 类型 | 说明 |
|------|------|------|------|
| `lang` | query | `string` | 可选 |

**响应字段（节选）**

| 字段 | 说明 |
|------|------|
| `id` | 分类 ID |
| `name` | 当前语言名称 |
| `description` | 当前语言描述 |
| `sort` | 排序 |
| `lang` | 实际使用的语言码 |
